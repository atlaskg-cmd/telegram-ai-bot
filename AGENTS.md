# AI Bot Project Context

## 📍 Где я работаю сейчас?

> **ТЕКУЩАЯ ЛОКАЦИЯ:** На работе (офис/рабочий ПК)  
> *Эта заметка меняется в зависимости от места работы*

### Контекст работы
| Место | Особенности |
|-------|-------------|
| **🏢 На работе** | Основной ПК, стабильный интернет, полный доступ |
| **🏠 Дома** | Личный ПК, нужен `git pull` перед работой, проверить WORK.md |

### Приоритеты задач
**Сейчас (на работе):**
- [ ] Активная разработка
- [ ] Тестирование новых фич
- [ ] Деплой на Railway

**Дома:**
- [ ] Доделать начатое на работе
- [ ] Тестирование
- [ ] Обновить WORK.md

---

## О проекте
Телеграм бот с ИИ (OpenRouter), погодой, новостями, TTS.

## Архитектура деплоя
```
VS Code → GitHub → Railway (auto-deploy)
```

## Важные правила

### 1. Бот остановлен локально
В `bot.py` есть защита:
```python
if os.environ.get('RAILWAY_ENVIRONMENT'):
    asyncio.run(main())  # только на Railway
else:
    print("[STOP] Bot stopped locally...")  # локально не запускается
```
**НЕ удалять эту проверку!**

### 2. Workflow изменений
```bash
# После любых изменений в коде:
git add .
git commit -m "описание изменений"
git push origin main
# Railway автоматически деплоит
```

### 3. Railway настройки
- **Auto Deploy**: ВКЛЮЧЕН (деплой после каждого push)
- **Start Command**: `python bot.py`
- **Переменные окружения**: 
  - `TELEGRAM_API_TOKEN` - токен бота от BotFather
  - `OPENROUTER_API_KEY` - API ключ OpenRouter
  - `WEATHER_API_KEY` - ключ OpenWeatherMap
  - `ADMIN_ID` - твой Telegram ID для админ-панели
  - `HF_TOKEN` - Hugging Face токен (FLUX.1, бесплатный tier)
  - `CF_API_TOKEN` - Cloudflare API Token (для генерации изображений)
  - `CF_ACCOUNT_ID` - Cloudflare Account ID (для генерации изображений)
  - `DATABASE_URL` - автоматически создаётся при добавлении PostgreSQL

### 4. Image Generation (FREE only)

Приоритет провайдеров (автоматический fallback):
1. **Hugging Face** - бесплатный tier, FLUX.1 schnell (отличное качество!)
2. **Cloudflare Workers AI** - если настроен (10k запросов/день)
3. **Pollinations.ai** - всегда бесплатно, без API ключа

#### Получение Hugging Face Token (бесплатно):
1. Перейди на https://huggingface.co/settings/tokens
2. Создай новый токен (Read access)
3. Добавь в Railway Variables как `HF_TOKEN`

### 5. Database (PostgreSQL) - ВАЖНО!
Бот поддерживает две базы данных:
- **SQLite** (`bot.db`) - локально, НЕ сохраняется при деплое!
- **PostgreSQL** - на Railway, данные сохраняются навсегда

#### Настройка PostgreSQL на Railway:
1. В Railway Dashboard нажми **"New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст переменную `DATABASE_URL`
3. Бот автоматически подключится к PostgreSQL (при наличии `DATABASE_URL`)
4. **ВСЕ ДАННЫЕ (контакты, настройки, история) будут сохраняться после деплоя!**

> ⚠️ Без PostgreSQL: контакты и настройки сбрасываются после каждого push!

### 5. Проверка ошибок
```bash
# Логи Railway (через CLI)
railway logs -f

# Или в браузере:
# https://railway.app/dashboard → проект → вкладка Logs
```

## Структура проекта
- `bot.py` — основной код бота
- `database.py` — база данных (SQLite/PostgreSQL)
- `config.json` — локальная конфигурация
- `requirements.txt` — зависимости
- `railway.json` — настройки Railway
- `Procfile` — команда запуска

## Git
- Репозиторий: `https://github.com/atlaskg-cmd/telegram-ai-bot.git`
- Ветка: `main`

---

## 🏠 Работа дома (инструкция)

Когда работаешь **дома** (личный ПК):

### Перед началом работы:
1. **Открой WORK.md** - там написано что делали на работе
2. **Выполни:**
   ```bash
   git pull
   ```
   *Это скачает все изменения сделанные на работе*

3. **Проверь статус:**
   ```bash
   git status
   ```

4. **Обнови AGENTS.md** - поменяй статус локации в начале файла:
   ```markdown
   > **ТЕКУЩАЯ ЛОКАЦИЯ:** Дома (личный ПК)
   ```

### Во время работы дома:
- Доделывай то, что начали на работе
- Обновляй WORK.md (секция "История изменений")
- Тестируй перед деплоем

### Перед уходом спать:
```bash
git add .
git commit -m "дома: описание изменений"
git push origin main
```

---

## 🏢 Работа на работе (инструкция)

Когда работаешь **на работе** (рабочий ПК):

### Перед началом работы:
1. **Проверь WORK.md** - что делали дома/вчера
2. **Выполни:**
   ```bash
   git pull
   ```

3. **Обнови AGENTS.md** - поменяй статус локации в начале файла:
   ```markdown
   > **ТЕКУЩАЯ ЛОКАЦИЯ:** На работе (офис/рабочий ПК)
   ```

### Перед уходом с работы:
```bash
git add .
git commit -m "работа: описание изменений"
git push origin main
```

---

## Примечания
- Бот работает ТОЛЬКО на Railway
- Локальный запуск запрещён (чтобы не было конфликта токенов)
- После изменений обязательно `git push origin main`
- Для сохранения данных настрой PostgreSQL (см. пункт 4)
- **WORK.md** - главный файл для отслеживания прогресса между ПК

---

## 🧠 AI Memory — История разговоров

> ⚠️ **ВАЖНО:** AI не помнит разговоры между сессиями. Этот раздел — память для новых сессий.

### 2026-02-12 — Работа на работе

**Тема:** Kimi CLI заканчивается, ищем альтернативы

**Обсудили:**
- Kimi Code CLI — пробный период заканчивается
- Лучшие альтернативы:
  1. **Aider** — бесплатный, open source, работает с GPT-4/Claude/DeepSeek
  2. **Ollama** — 100% бесплатно, локальные модели
  3. **GitHub Copilot CLI** — $10/мес
- Выбор: **Aider + DeepSeek API** (~$2/мес, дёшево и мощно)

**Решения:**
- ✅ Создать систему памяти (AI_MEMORY в AGENTS.md)
- ✅ git status проверили — всё актуально
- ⏳ Нужно протестировать Aider

**Следующий шаг:** Установить и настроить Aider

### 2026-02-11 — Работа на работе

**Тема:** Релиз версии 2.3.0

**Сделано:**
- ✅ Crypto Tracker с портфелем (CoinGecko API)
- ✅ Расширенная админ-панель (баны, управление админами)
- ✅ Исправлены баги PostgreSQL
- ✅ Настроена система git

**Версия:** 2.3.0
**Статус:** Готово к пушу

---

## 📝 Как использовать память

1. **Перед началом работы** — я читаю этот раздел
2. **Во время работы** — записываем важные решения здесь
3. **Перед `git push`** — обновляем этот раздел

**Команды для AI:**
- `запомни: [что-то важное]` — добавлю в память
- `что мы делали вчера?` — прочитаю историю
- `покажи последние решения` — покажу последние записи
