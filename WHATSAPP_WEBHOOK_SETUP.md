# Настройка Webhook для Green API (WhatsApp)

## 🚀 Быстрый старт (автоматическая настройка)

### Способ 1: Автоматический скрипт (рекомендуется)

После деплоя на Railway запустите скрипт настройки:

```bash
python setup_whatsapp_webhook.py
```

Этот скрипт автоматически:
- ✅ Проверит credentials (GREEN_API_ID, GREEN_API_TOKEN)
- ✅ Настроит webhook URL в Green API
- ✅ Включит все необходимые уведомления
- ✅ Проверит статус инстанса (QR-код)
- ✅ Выведет отчет о настройке

### Способ 2: Диагностика проблем

Если бот не отвечает, запустите диагностику:

```bash
python diagnose_whatsapp_webhook.py
```

Диагностика проверит:
- 🔍 Наличие credentials
- 🔍 Статус авторизации инстанса
- 🔍 Настройки webhook
- 🔍 Railway domain
- 🔍 Доступность endpoint

---

## 📝 Ручная настройка (если нужно)

### Шаг 1: Получение URL для Webhook

После деплоя на Railway ваш webhook URL будет:
```
https://<ваше-имя-проекта>.up.railway.app/webhook-whatsapp
```

Найти URL можно в Railway Dashboard → Settings → Public Domain

### Шаг 2: Настройка в консоли Green API

1. Перейдите в [консоль Green API](https://console.green-api.com/)
2. Выберите ваш инстанс
3. Нажмите "Настройки" или "Settings"
4. Найдите раздел "Webhook" или "Настройка вебхуков"
5. Введите URL webhook:
   ```
   https://<ваше-имя-проекта>.up.railway.app/webhook-whatsapp
   ```
6. Убедитесь, что включены следующие типы уведомлений:
   - ✅ incomingMessageReceived (получение входящих сообщений)
   - ✅ outgoingMessageStatus (статус отправки сообщений)
   - ✅ stateInstanceChanged (изменение статуса инстанса)
   - ✅ outgoingMessageWebhook
   - ✅ outgoingAPIMessageWebhook
7. Убедитесь что **incomingBlock = no** (не блокировать входящие)

---

## 🔧 Переменные окружения Railway

Убедитесь, что установлены:

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `GREEN_API_ID` | ID инстанса Green API | ✅ Да |
| `GREEN_API_TOKEN` | Токен инстанса Green API | ✅ Да |
| `TELEGRAM_API_TOKEN` | Токен Telegram бота | ✅ Да |
| `RAILWAY_PUBLIC_DOMAIN` | Домен Railway (автоматически) | ⚠️ Railway |
| `ADMIN_ID` | Ваш Telegram ID | ❌ Опционально |

---

## 🧪 Проверка настройки

### 1. Проверка статуса инстанса

Убедитесь что QR-код отсканирован:
- Зайдите в https://console.green-api.com/
- Статус должен быть "Авторизован" (зеленый)

### 2. Тестирование бота

Отправьте сообщение на номер WhatsApp бота:
- `привет` - должно показать меню
- `юань` - должно запросить сумму для конвертации

### 3. Проверка логов Railway

```bash
railway logs -f
```

Должны увидеть:
```
WhatsApp Webhook bot initialized
Webhook received: {...}
Processing incoming message from webhook
```

---

## ❌ Устранение проблем

### Бот не отвечает на сообщения

**Причина 1:** Webhook не настроен
```bash
python setup_whatsapp_webhook.py
```

**Причина 2:** QR-код не отсканирован
- Зайдите в https://console.green-api.com/
- Отсканируйте QR-код через WhatsApp

**Причина 3:** Неправильный webhook URL
- Проверьте RAILWAY_PUBLIC_DOMAIN
- Webhook должен быть: `https://your-domain.up.railway.app/webhook-whatsapp`

### Webhook настроен, но сообщения не приходят

Проверьте настройки в Green API Console:
- `incomingWebhook` = `yes`
- `webhookUrl` = правильный URL
- Статус инстанса = "Авторизован"

### Ошибки в логах Railway

```bash
railway logs -f
```

Если видите `401 Unauthorized` - неправильные credentials  
Если видите `404 Not Found` - неправильный webhook URL  
Если видите `Bot disabled` - credentials не установлены

---

## 📞 Дополнительная помощь

1. **Документация Green API:** https://green-api.com/docs/
2. **Консоль Green API:** https://console.green-api.com/
3. **Диагностика:** `python diagnose_whatsapp_webhook.py`

---

## 🎯 Контрольный чеклист

Перед запуском убедитесь:

- [ ] Green API credentials получены
- [ ] QR-код отсканирован (статус "Авторизован")
- [ ] Railway variables установлены (GREEN_API_ID, GREEN_API_TOKEN)
- [ ] Бот задеплоен на Railway
- [ ] Webhook настроен (автоматически или вручную)
- [ ] Тестовое сообщение отправлено

**Готово!** 🎉 Бот должен отвечать на WhatsApp сообщения.
